

services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: clinic-db
    ports:
      - "1433:1433"
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=ClinicApp!Password123
    volumes:
      - mssql-data:/var/opt/mssql

volumes:
  mssql-data:
